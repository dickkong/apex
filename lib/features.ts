// Feature flags. Set SUPPORT_ENABLED back to true to re-activate the in-app
// support tickets flow (nav button + /support page). The underlying pipeline —
// tables, actions, and the Oversight ticket inbox — stays live either way.
export const SUPPORT_ENABLED = false;